apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  template:
    spec:
      containers:
        - name: container
          env:
            - name: CONTAINERSTAGE_VARIABLE
              value: "I am container stage."
